version: '3'

services:

  react:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "3003:3000"
    environment:
      - BACKEND=172.30.0.2
    networks:
      demo_net:
        ipv4_address: 172.30.0.10

networks:
  demo_net:
    external: true
